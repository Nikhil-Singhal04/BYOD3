# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars.json

# Terraform plan output files (generated by CI)
*.tfplan
tfplan
plan-*.tfplan

# Local CLI artifacts
*.log
.DS_Store
Thumbs.db

# (Recommended) Keep the provider lockfile tracked for reproducible builds:
# .terraform.lock.hcl
